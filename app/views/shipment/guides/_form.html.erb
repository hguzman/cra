<%= form_for([@shipment, @guide], role: "form") do |f| %>
  <% if @guide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guide.errors.count, "error") %> prohibited this shipment from being saved:</h2>

      <ul>
      <% @guide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :radicado %>
    <%= f.text_field :radicado, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :destinatario %>
    <%= f.text_field :destinatario, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :direccion %>
    <%= f.text_field :direccion, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :ciudad %>
    <%= f.text_field :ciudad, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :peso %>
    <%= f.text_field :peso, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :observaciones %>
    <%= f.text_area :observaciones, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>