<div class="container">
  <div class="row">
    <div class="col-md-12">

      <h1>Envios</h1>

      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Fecha envio at</th>
            <th>Comments</th>
            <th colspan="3">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% @shipments.each do |shipment| %>
            <tr>
              <td><%= shipment.fecha_envio_at %></td>
              <td><%= shipment.comments %></td>
              <td><%= link_to 'Ver', shipment %></td>
              <td><%= link_to 'Editar', edit_shipment_path(shipment) %></td>
              <td><%= link_to 'Borrar', shipment, method: :delete, data: {  confirm: '¿Esta usted seguro?', mensaje: 'El envio se borrara definitivamente ' },class: "btn btn-danger btn-xs" %></td>
              <td>

            </tr>
          <% end %>
        </tbody>
      </table>


      <br>

      <%= link_to 'Nuevo envio', new_shipment_path %>
      
    </div>
  </div>
</div>
